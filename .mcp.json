{
  "mcpServers": {
    "notion": {
      "command": "sh",
      "args": ["-c", "set -a && [ -f \"$PWD/.env\" ] && . \"$PWD/.env\" && set +a && exec npx -y @notionhq/notion-mcp-server"],
      "env": {}
    }
  }
}
